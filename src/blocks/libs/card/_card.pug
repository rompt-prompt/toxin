mixin card(cls, footer = '')
    .card(class= `${cls}`)
        block
        if footer === 'login'
            .card__footer 
                span.text Уже есть аккаунт на Toxin
                +btn('Войти', 'btn_theme_ligth w99')
        else if footer === 'create'
            .card__footer 
                span.text Нет аккаунта на Toxin?
                +btn('Создать', 'btn_theme_ligth w99')
